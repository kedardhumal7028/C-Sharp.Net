using System;

namespace DS1
{
    class Program
    {
        // Node class (like struct in C)
        class Node
        {
            public int data;
            public Node next;
        }

        static void Main(string[] args)
        {
            // Head node initialized to null
            Node Head = null;

            Console.WriteLine("Linked List Head is initialized to null.");
        }
    }
}
